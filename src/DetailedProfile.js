export const renderDetailedProfile = (stats) => {\n  const container = document.createElement('div');\n  container.className = 'detailed-profile';\n  container.innerHTML = '<h3>Transaction Breakdown</h3><div id="status-chart"></div>';\n  return container;\n};
export const updateStatusChart = (counts) => {\n  const chart = document.getElementById('status-chart');\n  const total = Object.values(counts).reduce((a, b) => a + b, 0) || 1;\n  chart.innerHTML = Object.entries(counts).map(([status, count]) => `\n    <div class='chart-row'>\n      <span>${status}</span>\n      <div class='bar-container'><div class='bar' style='width: ${(count/total)*100}%'></div></div>\n      <span>${count}</span>\n    </div>\n  `).join('');\n};
