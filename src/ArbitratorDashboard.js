export const renderArbitratorDashboard = () => {\n  const dashboard = document.createElement('section');\n  dashboard.className = 'arbitrator-dashboard';\n  dashboard.innerHTML = '<h3>Arbitrator Dashboard</h3><div id="disputed-list">Loading disputes...</div>';\n  return dashboard;\n};
export const updateDisputedList = (disputes) => {\n  const list = document.getElementById('disputed-list');\n  if (disputes.length === 0) { list.innerHTML = '<p>No active disputes</p>'; return; }\n  list.innerHTML = disputes.map(tx => `\n    <div class='dispute-item'>\n      <span>Tx #${tx.id}</span>\n      <span>Sender: ${tx.sender}</span>\n      <span>Recipient: ${tx.recipient}</span>\n      <button onclick='window.resolveDispute(${tx.id}, "${tx.sender}")'>Award Sender</button>\n      <button onclick='window.resolveDispute(${tx.id}, "${tx.recipient}")'>Award Recipient</button>\n    </div>\n  `).join('');\n};
