  it('should not allow non-sender to add a milestone', () => {\n    simnet.callPublicFn('Conquistador', 'create-transaction', [Cl.principal(wallet2), Cl.uint(1000000)], wallet1);\n    const { result } = simnet.callPublicFn('Conquistador', 'add-milestone', [Cl.uint(1), Cl.uint(1), Cl.stringAscii('Fraudulent Milestone'), Cl.uint(500000)], wallet2);\n    expect(result).toBeErr(Cl.uint(1)); // ERR-UNAUTHORIZED\n  });
